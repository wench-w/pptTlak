Metadata-Version: 2.4
Name: ppt-talk-builder
Version: 0.1.0
Summary: 从 PPT 备注生成语音并合成带字幕的演讲视频的工具
Author-email: AutoGenerated <auto@example.com>
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: typer>=0.12.0
Requires-Dist: python-pptx>=0.6.23
Requires-Dist: moviepy<2.0,>=1.0.3
Requires-Dist: pydub>=0.25.1
Requires-Dist: mutagen>=1.47.0
Requires-Dist: edge-tts>=6.1.0
Requires-Dist: srt>=3.5.2
Requires-Dist: tqdm>=4.66.4
Requires-Dist: python-dotenv>=1.0.1
Requires-Dist: streamlit>=1.39.0

# PPT Talk Builder

使用 Python 将 PPT 幻灯片与备注转换成带字幕的演讲视频。流程：

1. 利用 LibreOffice（或自行导出的 PNG）获取每页幻灯片的图像。
2. 读取每页备注，调用 `edge-tts` 将文字转成语音。
3. 根据音频时长拼接幻灯片，合成视频。
4. 输出 `.srt` 字幕文件（包含备注文本），播放器可直接加载，或使用 `--burn-subtitles` 生成内嵌字幕版本。

## 环境准备（Conda）

```bash
conda env create -f environment.yml
conda activate ppt-talk-builder
```

若 Conda 安装包不可用，可退回 `pip install -r requirements.txt`（已由 `pyproject` 描述）。

## 依赖组件

- LibreOffice：负责将 PPT/PPTX 转成 PNG（也可自行提供图片目录）。
- FFmpeg：`moviepy` 依赖，用于写入 H.264 + AAC。
- Edge TTS：微软 Edge 在线语音服务，默认使用 `zh-CN-XiaoyiNeural`。

## 快速开始

```bash
ppt-talk-builder build \
  --ppt /path/to/slides.pptx \
  --workdir ./build \
  --voice zh-CN-XiaoyiNeural \
  --burn-subtitles
```

常用参数：

| 参数 | 描述 |
| --- | --- |
| `--ppt` | PPT/PPTX 文件路径 |
| `--workdir` | 中间结果（图片、音频、字幕、视频）输出位置 |
| `--libreoffice` | LibreOffice 可执行文件（默认 `libreoffice`） |
| `--voice` | TTS 发音人 |
| `--rate` | TTS 语速（例如 `+15%`） |
| `--burn-subtitles` | 是否在视频中绘制字幕（依赖 ImageMagick） |

执行完后将在 `workdir` 中看到：

- `slides/`：PNG 幻灯片。
- `audio/`：语音文件。
- `subtitles/talk.srt`：字幕文件。
- `talk.mp4`：最终视频；若启用内嵌字幕会生成 `talk_with_subtitles.mp4`。

## 图形界面（Streamlit）

如果希望通过界面上传 PPT、配置输出目录与语音信息，可运行：

```bash
conda activate ppt-talk-builder
streamlit run streamlit_app.py
```

界面说明：

- 左侧边栏配置发音人、语速/音量、是否烧录字幕以及 LibreOffice 路径。
- 主区域上传 PPT/PPTX 文件，填写输出目录，点击“开始生成”即可。
- 生成完成后可直接看到 manifest 信息并下载 `talk.mp4`、`talk_with_subtitles.mp4`（若勾选烧录）以及 `talk.srt`。

## 项目结构

```
ppt_talk_builder/
  cli.py           # Typer CLI
  models.py        # 数据模型
  ppt_reader.py    # PPT -> notes + slides
  tts_engine.py    # Edge TTS 封装
  audio_utils.py   # 音频工具
  subtitles.py     # 字幕写入
  video_builder.py # 利用 moviepy 合成视频
```

## 未来改进

- 支持本地 TTS 引擎（如 VITS/Bark）。
- 增加批量任务与并行处理。
- 提供 GUI。

